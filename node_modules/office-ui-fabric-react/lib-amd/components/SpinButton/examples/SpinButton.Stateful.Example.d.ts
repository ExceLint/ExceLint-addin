/// <reference types="react" />
import * as React from 'react';
export declare class SpinButtonStatefulExample extends React.Component<any, any> {
    render(): JSX.Element;
    private _hasSuffix(value, suffix);
    private _removeSuffix(value, suffix);
}
