/// <reference types="react" />
import * as React from 'react';
import { IExtendedPersonaProps } from '../SelectedPeopleList/SelectedPeopleList';
export interface IPeopleSelectedItemsListExampleState {
    currentSelectedItems: IExtendedPersonaProps[];
    controlledComponent: boolean;
}
export declare class PeopleSelectedItemsListExample extends React.Component<{}, IPeopleSelectedItemsListExampleState> {
    private _selectionList;
    private index;
    private selection;
    constructor(props: {});
    render(): JSX.Element;
    private _toggleControlledComponent;
    private _renderExtendedPicker();
    private _onRenderItem;
    private _setComponentRef;
    private _onAddItemButtonClicked;
    private _onItemDeleted;
    private _onExpandItem;
    private _onSelectionChange();
    private _onCopyItems(items);
    private _getExpandedGroupItems(item);
}
