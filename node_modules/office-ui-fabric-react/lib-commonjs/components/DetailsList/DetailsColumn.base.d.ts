/// <reference types="react" />
import { BaseComponent } from '../../Utilities';
import { IDetailsColumnProps } from './DetailsColumn.types';
export declare class DetailsColumnBase extends BaseComponent<IDetailsColumnProps> {
    private _root;
    private _dragDropSubscription;
    private _classNames;
    constructor(props: IDetailsColumnProps);
    render(): JSX.Element;
    componentDidMount(): void;
    componentWillUnmount(): void;
    componentDidUpdate(): void;
    private _onRenderColumnHeaderTooltip;
    private _onColumnClick(column, ev);
    private _getColumnDragDropOptions();
    private _hasAccessibleLabel();
    private _renderAccessibleLabel();
    private _onDragStart(item?, itemIndex?, selectedItems?, event?);
    private _onDragEnd(item?, event?);
    private _updateHeaderDragInfo(itemIndex, event?);
    private _onColumnContextMenu(column, ev);
    private _onRootMouseDown;
}
