/// <reference types="react" />
import * as React from 'react';
import { IColumn } from 'office-ui-fabric-react/lib/DetailsList';
import './Announced.Example.scss';
export interface IFileExampleItem {
    key: string;
    name: string;
    modified: string;
    modifiedby: string;
    filesize: string;
}
export declare class AnnouncedBulkOperationsExample extends React.Component<{}, {
    items: IFileExampleItem[];
    columns: IColumn[];
    numberOfItems: number;
}> {
    private _selection;
    private _dragDropEvents;
    private _draggedItem;
    private _draggedIndex;
    constructor(props: {});
    render(): JSX.Element;
    private _renderAnnounced();
    private _getDragDropEvents();
    private _onItemInvoked(item);
    private _onRenderItemColumn(item, index, column);
    private _insertBeforeItem(item);
}
