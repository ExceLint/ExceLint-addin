let { Text, Fabric } = window.Fabric;

const TextInheritsExample = () => (
  <>
    <Text variant={'large'}>
      The following text inherits its font styling from its parent: <Text>text</Text>.
    </Text>
  </>
);

ReactDOM.render(<TextInheritsExample />, document.getElementById('content'));
