/// <reference types="react" />
import { BaseComponent } from '../../Utilities';
import { ILink, ILinkProps } from './Link.types';
export declare class LinkBase extends BaseComponent<ILinkProps, any> implements ILink {
    private _link;
    render(): JSX.Element;
    focus(): void;
    private _onClick;
    private _adjustPropsForRootType(RootType, props);
    private _getRootType(props);
}
