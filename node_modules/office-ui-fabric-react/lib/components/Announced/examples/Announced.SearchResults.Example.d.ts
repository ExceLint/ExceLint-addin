/// <reference types="react" />
import * as React from 'react';
export interface IAnnouncedSearchResultsExampleState {
    seconds: number;
    numberOfSuggestions: number;
    emptyInput: boolean;
}
export interface IAnnouncedSearchResultsExampleProps {
}
export declare class AnnouncedSearchResultsExample extends React.Component<IAnnouncedSearchResultsExampleProps, IAnnouncedSearchResultsExampleState> {
    private timer;
    private _async;
    constructor(props: {});
    render(): JSX.Element;
    componentWillUnmount(): void;
    private _renderAnnounced();
    private _getTextFromItem(item);
    private _onFilterChanged;
    private _listContainsDocument(tag, tagList);
}
